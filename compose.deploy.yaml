services:
  sub2api-mcp:
    build:
      args:
        UV_IMAGE: ghcr.io/astral-sh/uv:0.11.1
        PYTHON_IMAGE: python:3.12.7-slim
    networks:
      - langbot

networks:
  langbot:
    name: ${LANGBOT_DOCKER_NETWORK:-docker_langbot_network}
    external: true
